ul.group-list.clearfix
  li.group-item(ng-repeat="group in groups", ng-click="joinGroup(group)", ng-style="wrapBgImgStyle(group.bgPath)")
    .group-desc(ng-class="{'has-bg': group.bgPath}")
      h3.group-name {{group.name}}
      small {{group.desc}}